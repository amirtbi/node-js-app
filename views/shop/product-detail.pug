extends ../layout.pug

block content 
    div.centered
        h1 #{product.title}
        hr
        div 
            img(src=product.imageUrl alt=product.title)
        h2 #{product.price}
        p #{product.description}
        include ../share/add-to-cart.pug